{
  "targets": [
    {
      "name": "server_pex",
      "cmd": "cd .. && ./scripts/build_pex.sh server",
      "inputs": ["server/**/*.py"]
    },
    {
      "name": "server_test",
      "cmd": "uv run pytest",
      "inputs": ["server/**/*.py", "tests/**/*.py"]
    }
  ]
}
