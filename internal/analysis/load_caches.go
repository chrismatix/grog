package analysis

import (
	"grog/internal/dag"
	"grog/internal/model"
)

// LoadCaches enriches the target graph with its cache information
func LoadCaches(graph *dag.DirectedTargetGraph) error {
	//for _, target := range graph.GetVertices() {
	//
	//}
	return nil
}

func loadCacheData(target *model.Target) error {
	//
	return nil
}
