name: Directory outputs
repo: dir_output
cases:
  - name: creates_directory_output
    grog_args:
      - build

  # This loads the directory output from the cache
  - name: loads_directory_output
    grog_args:
      - build
