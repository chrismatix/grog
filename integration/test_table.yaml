cases:
  - name: no_arguments
    repo: empty
  - name: build_all
    args:
      - build
    repo: simple_json
  - name: build_all_color
    args:
      - --color=yes
      - build
    repo: simple_json
  - name: build_foo
    args:
      - build
      - //foo
    repo: simple_json
