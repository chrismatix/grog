targets:
  # Workspace dependencies come from uv.lock, so none are listed here.
  - name: sources
    inputs: ["**/*.py", "pyproject.toml"]
    command: echo checking format
    dependency_resolvers: ["//:uv"]
