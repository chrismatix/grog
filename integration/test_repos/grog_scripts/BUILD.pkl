amends ".../pkl/package.pkl"

targets {
  new {
    name = "some_target"
    inputs {
      "some_target.sh"
    }
    bin_output = "some_target.sh"
    tags {
      "no-cache"
    }
  }

  new {
    name = "a_build_target"
    command = "mkdir -p dist && echo \"foo\" > dist/output.txt"
  }
}
