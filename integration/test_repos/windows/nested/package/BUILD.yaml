targets:
  - name: build
    dependencies: ["//:app", "//:service"]
    command: |
      mkdir -p result
      printf '%s\n' "$SERVICE_VALUE" > result/value.txt
      "$(bin //:app)" 'two words' > result/arguments.txt
    outputs: ["dir::result"]
