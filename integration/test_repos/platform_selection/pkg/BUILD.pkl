amends ".../pkl/package.pkl"

targets {
  new {
    name = "foo"
    command = "echo 'hello world'"
  }

  // this should be silently skipped when running
  // grog build //pkg/...
  new {
    name = "foo_platform"
    command = "echo 'hello world'"
    platforms {
      "made-up-os/made-up-arch"
    }
  }
}
