targets:
  # No dependencies are listed: the resolver derives them from the imports.
  - name: generate
    inputs: ["*.proto"]
    command: echo generating user
    dependency_resolvers: ["//:protos"]
